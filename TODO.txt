strings -> data, key
languages -> languageConfig optional

Flow
pass fallback object
provider recibe locale
change, locale, load language on demand

locale
return locales keys
getstaticparams <- locales
provider -> prop locale keyof locales

******
on language change update translations
check is ready Flow

version 0.2.0
documentación